# Backend

This folder will contain the FastAPI backend application.

The backend will receive telemetry data, validate it, store it in the database, expose APIs to the frontend, and coordinate AI assistant requests.
